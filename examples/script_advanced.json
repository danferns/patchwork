{
  "name": "Math Operations",
  "inputs": [
    "x",
    "y",
    "mode"
  ],
  "outputs": [
    "result",
    "absolute"
  ],
  "code": "let diff = x - y;\nlet abs_val = if diff < 0.0 { -diff } else { diff };\nlet out = if mode < 0.5 { x + y } else if mode < 1.5 { x * y } else { diff };\noutputs[0] = out;\noutputs[1] = abs_val;"
}
